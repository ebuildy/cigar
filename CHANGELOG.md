# Changelog

All notable changes to this project are documented here, generated from
Conventional Commits by [git-cliff](https://git-cliff.org) — see `mise r changelog` / `mise r release:tag`.
## [Unreleased]

### 🚀 Features

- Initial commit ([dcbe377])
- Initial commit ([caa7aff])
- Add helm chart ([9324dce])
- *(config)* Parse AUTH_METHODS and WEBHOOK_SIGNING_TOKEN ([2ba9d38])
- *(webhook)* Add Authenticator with secret and signature verifiers ([d0fcddc])
- *(webhook)* Authenticate via ordered Authenticator list ([6cd2121])
- *(bot)* Build ordered webhook authenticators from AUTH_METHODS ([15c5390])
- *(webhook)* Auth signing token ([e3a91eb])
- *(gitlab)* Add JobTrace to fetch a job's raw trace log ([936f667])
- *(correlate)* Add GitLab-trace pod resolver ([93bef59])
- *(config)* Add POD_RESOLVER (default trace) ([86b9373])
- *(bot)* Select pod resolver from POD_RESOLVER ([a0966dd])
- *(report)* Error notice when no runner pods correlate to any job ([357600d])
- *(config)* COMMANDS_ENABLED and COMMANDS_SIGNING_KEY ([12a01a9])
- *(report)* HMAC-signed marker (sign, parse, prefix lookup) ([6117cc3])
- *(report)* Render honors a signed-marker override ([a514659])
- *(reporter)* Sign the note marker, look up existing note by prefix ([1359486])
- *(command)* Parse help and details [job|pod] <name> ([c549a4d])
- *(metrics)* PodSeries and PodActiveSpan range queries ([81d9ba4])
- *(gitlab)* CurrentUser, MergeRequestDiscussion, UploadFile, CreateDiscussionReply ([ec4f3d5])
- *(chart)* Sanitizer-safe SVG line-chart renderer ([38b2077])
- *(command)* Handler authorization (author gate, HMAC, loop guard) + help ([f41f162])
- *(command)* Details execution with live allowlist and 3 SVG charts ([45269a6])
- *(webhook)* Event union and Note Hook routing gated by commandsEnabled ([7ddcea6])
- *(serve)* Wire command handler, note dedup, generalized queue ([0ec128a])
- *(chart)* PNG charts (default) with configurable CHART_FORMAT ([9b166d3])
- *(chart)* Markdown format — inline ASCII line chart, no upload ([71c5142])
- *(run)* --job flag prints one job's usage details to stdout ([fbfceaa])
- *(chart)* Human-readable y-axis units in markdown charts ([80c95a2])
- *(chart)* CPU axis in millicores below one core ([95d5919])
- *(chart)* Wider markdown x-axis with start time + duration labels ([a8bcd8f])
- *(chart)* Chart only network TX (drop RX) ([1925dbc])
- *(advice)* Rule interface, registry and engine ([12297e2])
- *(advice)* Cpu-throttle rule ([2863351])
- *(advice)* Java-threads rule with trace-based build-tool detection ([3328320])
- *(advice)* Long-job rule ([1fea9af])
- *(advice)* Memory-pressure rule ([3688d74])
- *(advice)* Markdown rendering with golden files ([1845d84])
- *(advice)* Tie the java thread count to the current CPU limit in copy ([9e3a747])
- *(config)* LONG_JOB_DURATION and MEMORY_PRESSURE_RATIO ([680c548])
- *(reporter)* Advise gathers facts and runs the advice engine ([8404e85])
- *(command)* Parse the advise command ([f0a522c])
- *(command)* Execute advise via the Advisor interface ([c0ae86c])
- *(bot)* Bot advise subcommand and serve-path advisor wiring ([796b1e4])
- *(config)* Add viper dep, settings table and name derivation ([7e9bf05])
- *(config)* Load non-secret values via viper, secrets env-only ([9d43ce6])
- *(config)* Bind CLI flags and resolve config file with precedence ([18a56fc])
- *(cmd)* Wire config.yaml + flags via viper in root command ([e23896a])
- *(chart)* Ship config.yaml as a mounted ConfigMap; secrets stay env ([d09f72e])
- Add metrics duration, disk io ([4417798])
- Add prom metrics endpoint ([c8f6299])
- *(helm)* Add external secret integration ([8629782])

### 🐛 Bug Fixes

- *(webhook)* Guard empty secret in secretAuth, add auth edge-case tests ([6581831])
- *(chart)* Gate WEBHOOK_SECRET/WEBHOOK_SIGNING_TOKEN env by enabled auth method ([a7cf8ed])
- *(correlate)* Strip all ANSI CSI escapes; satisfy gitlab.Client in reporter test ([73bd046])
- *(chart)* Restore SVG xmlns; guard real external-ref vectors in test ([b00c4f0])
- *(command)* Marker-based loop guard so a shared/human token account works ([88292ac])
- *(metrics)* Widen chart rate window to 4x scrape; fix dev SCRAPE_INTERVAL ([247d96a])
- *(advice)* Deterministic unknown-rule error, empty selection means all rules ([45db5f5])
- *(advice)* Clamp suggested CPU request to suggested limit ([0233766])
- *(advice)* Tool-specific java-threads snippets, ANSI-safe trace matching ([d9cb84c])
- *(gitlab)* Repost report as a new note once its thread has replies ([4989931])

